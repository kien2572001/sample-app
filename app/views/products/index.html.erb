<p style="color: green"><%= notice %></p>

<h1><%= t("products.index.title") %></h1>

<table>
  <thead>
    <tr>
      <th><%= t("products.attributes.name") %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <%= render (@products) || t("products.index.no_products") %>
  </tbody>
</table>

<br>

<%= link_to t("products.others.new"), new_product_path %>
